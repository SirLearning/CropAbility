# CropAbility — 核心依赖
# 安装: pip install -r requirements.txt

# 深度学习框架（GPU 版本由 CUDA 驱动决定）
torch>=2.1.0

# GPU 内核编译
triton>=2.1.0

# 数值计算
numpy>=1.24.0

# 配置文件支持
pyyaml>=6.0
tomli>=2.0.0; python_version < "3.11"

# 测试
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-xdist>=3.3.0

# 代码质量
ruff>=0.1.0

# 可视化（可选）
matplotlib>=3.7.0
seaborn>=0.12.0

# 文档（可选）
# sphinx>=7.0
# sphinx-rtd-theme>=1.3
